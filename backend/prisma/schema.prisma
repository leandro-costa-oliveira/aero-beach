// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:./aerobeach.db"
}

model Contador {
  id       Int @id @default(autoincrement())
  contador Int
}

enum TipoTorneio {
  masculino
  feminino
  misto
}

enum CategoriaTorneio {
  iniciante
  d
  c
  b
  a
  open
}

enum SituacaoTorneio {
  aberto
  cancelado
  realizando
  concluído
}

enum ModalidadeTorneio {
  duplas
  simples
}

enum Role {
  user
  player
  admin
}

model Usuario {
  id       String  @id @default(uuid())
  nome     String
  email    String  @unique
  role     Role    @default(user)
  telefone String?
  senha    String?
  salt     String?

}

model Jogador {
  id       String  @id @default(uuid())
  nome     String
  email    String  @unique
  cpf      String?
  telefone String?

  inscricoes Inscricao[]
  duplas1    Duplas[]     @relation("duplaParticipante1")
  duplas2    Duplas[]     @relation("duplaParticipante2")

}

model Inscricao {
  id            String    @id @default(uuid())
  torneioId     String
  jogadorId     String
  pago          Boolean   @default(false)
  dataPagamento DateTime?
  categoriasId  String

  torneio    Torneio    @relation(fields: [torneioId], references: [id])
  jogador    Jogador   @relation(fields: [jogadorId], references: [id])
  categoria Categoria? @relation(fields: [categoriasId], references: [id])
}

model Duplas {
  id            String @id @default(uuid())
  torneioId     String
  participante1 String
  participante2 String
  categoriasId  String

  torneio    Torneio   @relation(fields: [torneioId], references: [id])
  jogador1   Jogador  @relation("duplaParticipante1", fields: [participante1], references: [id])
  jogador2   Jogador  @relation("duplaParticipante2", fields: [participante2], references: [id])
  categoria Categoria @relation(fields: [categoriasId], references: [id])

  @@unique([participante1, participante2, categoriasId])
}

model Torneio {
  id                  String          @id @default(uuid())
  nome                String?
  dataInicio          DateTime
  dataLimiteInscricao DateTime
  federado            Boolean         @default(false)
  situacao            SituacaoTorneio @default(aberto)

  categoria Categoria[]
  inscricoes Inscricao[]
  duplas     Duplas[]
}

model Categoria {
  id             String            @id @default(uuid())
  torneioId      String
  genero         TipoTorneio
  modalidade     ModalidadeTorneio
  nivel          CategoriaTorneio
  valorInscricao Float
  dataRealizacao DateTime?

  torneio    Torneio     @relation(fields: [torneioId], references: [id])
  inscricoes Inscricao[]
  duplas     Duplas[]

  @@unique([torneioId, genero, modalidade, nivel])
}
