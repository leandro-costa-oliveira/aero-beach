// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:./aerobeach.db"
}

model Contador {
  id       Int @id @default(autoincrement())
  contador Int
}

model Torneios {
  id                    String            @id @default(uuid())
  nome                  String?
  data                  DateTime
  realizado_em          DateTime?
  data_limite_inscricao DateTime
  valor_inscricao       Float
  tipo                  TipoTorneio
  categoria             CategoriaTorneio
  modalidade            ModalidadeTorneio
  federado              Boolean           @default(false)

  inscricoes Inscricoes[]
}

enum TipoTorneio {
  masculino
  feminino
  misto
}

enum CategoriaTorneio {
  iniciante
  d
  c
  b
  a
  open
}

enum ModalidadeTorneio {
  duplas
  simples
}

model Jogadores {
  id       String @id @default(uuid())
  nome     String
  cpf      String
  telefone String
  email    String

  inscricoes Inscricoes[]
  duplas1    duplas[]     @relation("duplaParticipante1")
  duplas2    duplas[]     @relation("duplaParticipante2")
}

model Inscricoes {
  id            String   @id @default(uuid())
  torneio_id    String
  jogador_id    String
  pago          Boolean  @default(false)
  dataPagamento DateTime

  torneio Torneios  @relation(fields: [torneio_id], references: [id])
  jogador Jogadores @relation(fields: [jogador_id], references: [id])
}

model duplas {
  id            String @id @default(uuid())
  participante1 String
  participante2 String

  jogador1 Jogadores @relation("duplaParticipante1", fields: [participante1], references: [id])
  jogador2 Jogadores @relation("duplaParticipante2", fields: [participante2], references: [id])

  @@unique([participante1, participante2])
}
